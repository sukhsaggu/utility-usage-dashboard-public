# Local XML samples (not in git)

Put your own **Green Button / ESPI** utility XML exports in this folder for quick local testing (drag-and-drop in the app). Filenames under `sample_files/` are **gitignored** so usage data never lands in the repository.

Examples of typical names: `EGD_Gas_EnergyUsage_*.xml`, provider non-interval interval files.
